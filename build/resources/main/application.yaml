spring:
  application:
    name: fairticket
    
  datasource:
    url: jdbc:postgresql://localhost:5430/fairticket
    username: postgres
    password: camilo123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        default_schema: public
        format_sql: true
